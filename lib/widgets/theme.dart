import 'package:flutter/material.dart';

// 定义一些常用的颜色
const primaryColor = Color(0xFF8BDEAC);  // 主色调
const secondaryColor = Color(0xFF2A2D3E);  // 次色调
const bgColor = Color(0xFF212332);  // 背景色

// 定义一个常用的内边距数值
const defaultPadding = 16.0;  // 默认内边距为16.0